define('master/collection/weather', [
	'config',
	'jquery',
	'underscore',
	'backbone',
	'router',
	'master/model/weather'

], function(config, $, _, Backbone, AppRouter, WeatherModel) {
	'use strict';
	
	var latitude;
	var longitude;
	var error 

	return Backbone.Model.extend({


	});
});